/**
 * Various defaults for the PTS application.
 */
Ext.define('PTS.Defaults', {
    singleton: true,

    /**
     * @cfg {Number}
     * The default page size to include in requests
     */
    pageSize: 25
});
